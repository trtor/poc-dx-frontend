export type SuggestionList = {
  rank: number;
  conceptId: string;
  term: string;
  pid: number;
};

export type DiagnosisRelation = {
  conceptId: string;
  term: string;
  pid: number;
};

/*
  Rank: number;
  conceptId: string;
  term: string;
  pid: number;
*/
